import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App'; //framer-motion 사용해보기 https://nykim.work/114

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>
);
